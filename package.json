{
  "name": "matrix-chat-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny WebSocket relay pairing exactly two clients for a terminal chat.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
